using UnityEngine;
using System.Collections;

public class AnguloDebug : MonoBehaviour {
    public static GUIText texto;
	// Use this for initialization
	void Start () {
        texto = GetComponent<GUIText>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
